<div class="container">
  <% flash.each do |type, message| %>
    <div class="alert alert-dismissable alert-<%= type == "error" ? 'danger' : 'success' %> fade in">
      <button class="close" data-dismiss="alert">×</button>
      <%= message %>
    </div>
  <% end %>
</div>